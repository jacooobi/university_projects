<%= form_for(@concert) do |f| %>
  <% if @concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% @concert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date, { include_blank: true } %>
  </div>

  <div class="field">
    <%= f.label :location_id %><br>
    <%= f.select :location_id, Location.all.collect { |f| [f.name, f.id] }, { include_blank: true } %>
  </div>

  <div class="field">
    <%= f.label :band_id %><br>
    <%= f.select :band_id, Band.all.collect { |f| [f.name, f.id] }, { include_blank: true } %>
  </div>

  <div class="field">
    <%= f.label :festival_id %><br>
    <%= f.select :festival_id, Festival.all.collect { |f| [f.name, f.id] }, { include_blank: true } %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
